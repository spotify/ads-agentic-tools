description = "Configure Spotify Ads API credentials (OAuth 2.0 with PKCE or direct token)"
prompt = """
Activate the spotify-ads-api `configure` Agent Skill and follow its instructions
exactly. If skill activation is unavailable, read and follow skills/configure/SKILL.md
from this extension's directory.

User arguments (mode selection: oauth [client_id] | token <access_token>): {{args}}
"""
